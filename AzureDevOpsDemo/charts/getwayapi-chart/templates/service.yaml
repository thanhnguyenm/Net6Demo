apiVersion: v1
kind: Service
metadata:
  name: {{ include "getwayapi-chart.fullname" . }}
  labels:
    {{- include "getwayapi-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "getwayapi-chart.selectorLabels" . | nindent 4 }}
